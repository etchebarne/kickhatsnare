/// Owns project/workspace state and persistence operations.
///
/// Session, track, timeline, and asset modules should be composed here as
/// those concepts are introduced.
#[derive(Debug, Default)]
pub struct Workspaces;
